#include <iostream>
using namespace std;

int main() {
    string str;

    cout << "Enter a string: ";
    getline(cin, str);

    int len = str.length();

    cout << "Reversed string: ";

    for(int i = len - 1; i >= 0; i--) {
        cout << str[i];
    }

    cout << endl;

    return 0;
}
